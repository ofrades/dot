{
  "describe each": {
    "prefix": "describeeach",
    "body": [
      "describe.each`",
      "\t${1:param1}        | ${3:param2}",
      "\t$${2:{'value1'}}     | $${4:{'value2'}}",
      "\t`('when $${1:param}', ({${1:param}, ${3:param2}}) => {",
      "\t${0}\n})"
    ]
  },
  "it each": {
    "prefix": "iteach",
    "body": [
      "it.each`",
      "\t${1:param1}        | ${3:param2}",
      "\t$${2:{'value1'}}     | $${4:{'value2'}}",
      "\t`('when $${1:param}', ({${1:param}, ${3:param2}}) => {",
      "\t${0}\n})"
    ]
  },
  "log": {
    "prefix": "logasobject",
    "body": "console.log({ ${0} })"
  }
}
